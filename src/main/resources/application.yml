server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://ep-misty-salad-a83txo1h-pooler.eastus2.azure.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_fdl1PmAiBh6F
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.megaproject: debug

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}